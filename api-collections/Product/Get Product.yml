info:
  name: Get Product
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/products/2"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
