info:
  name: Get Products
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/products"
  body:
    type: multipart-form
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
