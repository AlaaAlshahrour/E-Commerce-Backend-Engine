info:
  name: update quantity
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{base_url}}/cart/update/3"
  body:
    type: multipart-form
    data:
      - name: quantity
        type: text
        value: "3"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
