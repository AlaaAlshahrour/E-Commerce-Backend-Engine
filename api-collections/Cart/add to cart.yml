info:
  name: add to cart
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/cart/add/21"
  body:
    type: multipart-form
    data:
      - name: quantity
        type: text
        value: "3"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
