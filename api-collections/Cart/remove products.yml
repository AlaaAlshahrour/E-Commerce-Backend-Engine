info:
  name: remove products
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{base_url}}/cart/remove"
  params:
    - name: ""
      value: ""
      type: query
  body:
    type: json
    data: |-
      {
        "product_ids":[3]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
