info:
  name: clear cart
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{base_url}}/cart/clear"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
