info:
  name: Update Category
  type: http
  seq: 2

http:
  method: PUT
  url: "{{base_url}}/categories/1"
  body:
    type: multipart-form
    data:
      - name: name
        type: text
        value: phones-alaa
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
